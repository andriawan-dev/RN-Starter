const authService = ( () => {

    const postLogin = async ({username,password}) => {
        try{
            
        }catch(e){

        }
    }

    return {
        postLogin: postLogin,
    }
})();

export default authService;